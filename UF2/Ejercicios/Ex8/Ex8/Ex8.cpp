/*	Heu de realitzar la llibreria (el .h i el .cpp) amb les funcions per tractar un vector d’enters sense importar la seva longitud.
	Les funcions que s’han d’implementar són:
		Una funció per introduir dades en el vector. Li passarem el vector i les posicions que volem introduir i retornarà el vector omplert. 
		Una funció per mostrar les dades del vector per pantalla. Li passarem el vector i la posició fins a la que volem mostrar.
		Una funció que retorni el vector ordenat de forma ascendent. Li passarem el vector i fins a quina posició volem ordenar.
		Una funció que retorni la mitja aritmètica dels elements del vector. Li passarem el vector i la posició fins a la qual volem calcular la mitja.
		Una funció que retorni l’enter mínim del vector. Li passarem el vector i la posició fins a la qual volem calcular el mínim.
		Una funció que retorni l’enter màxim del vector. Li passarem el vector i la posició fins a la qual volem calcular el màxim.
		Una funció per copiar un vector en un altre vector. Li passarem el vector que volem copiar i el vector on volem copiar.
		Una funció per buscar un element en el vector. Li passarem el vector, fins a quina posició volem buscar i l’element que volem buscar.
			Ens ha de retornar un booleà per dir si l’element existeix en el vector o no i la posició dintre del vector on es troba l’element. 
	Heu de fer un programa per poder provar totes les funcions programades en la llibreria. 
*/

#include <stdio.h>
#include <stdlib.h> // Memòria dinàmica
#include <vector>	// Llibreria personalitzada

void main()
{
	int vector[1];

	//vector = (int *)malloc(sizeof(int));
	
}
